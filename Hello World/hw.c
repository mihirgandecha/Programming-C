#include <stdio.h> //These are called preprocessing directives

int main(void) //main stage
{ //Statements are grouped using braces
    printf("Hello, World!\n"); /*Pre-defined library functions being called (or invoked) using a single arguement
    in the string*/
//\n means new line //
    return 0; //declarations are terminated using ; return 0 means main() has been completed
}

/*Comments can be written in this pair*/